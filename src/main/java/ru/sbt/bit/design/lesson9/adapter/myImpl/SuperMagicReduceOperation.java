package ru.sbt.bit.design.lesson9.adapter.myImpl;

public class SuperMagicReduceOperation {

    int doTheMagic(int x, int y, ReduceSettings settings) {
        // here is some magic happening
        return Math.min(x, y);
    }
}
